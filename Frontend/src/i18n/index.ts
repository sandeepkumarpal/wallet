import {
  LANG_COOKIE,
  LANG_STORAGE_KEY,
  SUPPORTED_LANG_CODES,
  SUPPORTED_LANGUAGES,
  normalizeLanguage,
  persistLanguage,
  type AppLanguage,
} from "./config";

export {
  LANG_COOKIE,
  LANG_STORAGE_KEY,
  SUPPORTED_LANG_CODES,
  SUPPORTED_LANGUAGES,
  normalizeLanguage,
  persistLanguage,
};
export type { AppLanguage };
